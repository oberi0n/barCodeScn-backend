services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      ALLOWED_ORIGINS: "${ALLOWED_ORIGINS:-https://192.168.1.186}"
    volumes:
      - ./data:/data
    restart: unless-stopped
